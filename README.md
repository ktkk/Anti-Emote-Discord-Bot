# Anti Emote Discord Bot
This is a simple bot that reacts to emote usage in Discord. It also does some other random stuff.

## Build and run
To build and run the jar, have gradle installed and simply:
```sh
gradle run
```
